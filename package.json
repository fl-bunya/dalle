{
  "name": "dalle",
  "version": "2.0.0",
  "description": "Slack bot that generates images from text using OpenAI's DALL-E",
  "main": ".src/app.js",
  "scripts": {
    "start": "node .src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.6.0",
    "@slack/web-api": "^6.9.1",
    "dotenv": "^16.3.1",
    "openai": "^4.17.3"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  }
}
